<%= form_for(@users_controller) do |f| %>
  <% if @users_controller.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@users_controller.errors.count, "error") %> prohibited this users_controller from being saved:</h2>

      <ul>
      <% @users_controller.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
